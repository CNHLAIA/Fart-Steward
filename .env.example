## Backend configuration (used by docker-compose)
##
## docker compose automatically loads a .env file in the project root.
## Copy this file to .env and adjust values as needed.

# Flask secret key used for session/security features.
SECRET_KEY=dev-secret-key

# JWT signing key for access tokens.
JWT_SECRET_KEY=dev-secret-key

# SQLite database path inside the backend container.
# Must live under /app/data to persist via the ./data bind mount.
SQLITE_PATH=/app/data/app.db
